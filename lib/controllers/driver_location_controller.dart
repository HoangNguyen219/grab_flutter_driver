import 'package:get/get.dart';

class DriverLocationController extends GetxController {
  // final _driverLocation = DriverLocationState().obs;
  //
  // set driverLocation(DriverLocationState value) => _driverLocation.value = value;
  // DriverLocationState get driverLocation => _driverLocation.value;
}
